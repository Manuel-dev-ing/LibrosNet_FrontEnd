

export function createArrayNums(total:number): number[] {
    
    let arr_nums = []
    for (let index = 1; index <= total; index++) {
        
        arr_nums.push(index)
    }

    return arr_nums
}

